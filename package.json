{
  "name": "apexfx-proxy",
  "version": "1.0.0",
  "description": "OANDA CORS proxy for APEXFX PWA",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
